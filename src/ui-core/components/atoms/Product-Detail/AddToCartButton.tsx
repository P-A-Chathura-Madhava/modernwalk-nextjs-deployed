import { Button } from "@/components/ui/button";
import React from "react";

function AddToCartButton() {
  return (
    <Button className="bg-[#db5c12] hover:bg-[#f57224] transition-all duration-300">
      Add to Cart
    </Button>
  );
}

export default AddToCartButton;
